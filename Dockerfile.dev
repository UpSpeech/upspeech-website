# Development Dockerfile for UpSpeech Marketing Website
# This runs the Vite dev server for hot reloading during development

FROM node:22-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy application code
COPY . .

# Expose Vite default port
EXPOSE 3052

# Start Vite dev server
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
