// Live store listing URLs. Operator: fill these in once each app is public,
// or leave "" to keep the corresponding store badge hidden. Shared by the
// footer and the /for-clients page so both surfaces switch on together.
export const APP_STORE_URL = "";
export const PLAY_STORE_URL = "";
